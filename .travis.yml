sudo: false
dist: focal
cache: pip
services:
  - redis-server
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
before_install:
  - pip install poetry
install: poetry install -v
script: python -m pytest
